import { CalcDiscountPipePipe } from './calc-discount-pipe.pipe';

describe('CalcDiscountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcDiscountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
